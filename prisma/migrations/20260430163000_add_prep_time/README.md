Manual migration file created because `prisma migrate dev --create-only` was blocked by existing shadow database migration issues (P3006/P1014 unrelated to this change).
This migration only adds `recipes.prepTime` and is intentionally not executed.
